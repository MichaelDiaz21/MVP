Para la ejecucion del frontend, desde la carpeta se abre la terminal y se coloca npm run dev 
Para la ejecucion del backend, desde la carpeta se abre la terminal y se coloca code ., y se ejecuta el comando npx nodemon app
se recomienda tener mongodb compass y crear la base de datos como se adjunta en el documento
